services:
  opa:
    image: openpolicyagent/opa:1.12.1
    command: run --server --watch --log-level debug --addr=0.0.0.0:8181 /policies
    ports:
      - "8181:8181"
    volumes:
      - ./policies:/policies:ro
